apiVersion: v1
kind: Service
metadata:
  name: express-4016-service
  namespace: hwang268
spec:
  type: LoadBalancer
  selector:
    app: express-4016
  ports:
  - name: http
    port: 30000
    protocol: TCP
    targetPort: 30000